[tool.pytest.ini_options]
# Keep consistent with pytest.ini; pyproject presence helps some CI environments
addopts = "-q --maxfail=1 --disable-warnings --cov=app --cov-report=term-missing --cov-report=html:htmlcov"
testpaths = ["tests"]

[tool.coverage.run]
source = ["app"]
branch = true
