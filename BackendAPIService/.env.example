# BackendAPIService environment template

# Preferred single variable
MONGODB_URI=mongodb://localhost:27017/network_devices

# Optional overrides
MONGODB_DB_NAME=network_devices
MONGODB_COLLECTION=device

# TLS and timeout
MONGODB_TLS=false
MONGODB_CONNECT_TIMEOUT_MS=5000

# Fallback parts (only used if MONGODB_URI is not set)
#MONGODB_HOST=localhost
#MONGODB_PORT=27017
#MONGODB_USERNAME=
#MONGODB_PASSWORD=
#MONGODB_OPTIONS=
