# Backend .env example (Flask + PyMongo)
# Copy this file to `.env` and adjust values as needed.

# MongoDB connection URI (required)
# Example for local MongoDB:
MONGO_URI=mongodb://localhost:27017

# Database name (optional; default: network_devices)
MONGO_DB_NAME=network_devices

# Enable TLS for Mongo connection (optional: true/false)
MONGO_TLS=false

# MongoClient connect timeout in milliseconds (optional)
MONGO_CONNECT_TIMEOUT_MS=10000
