# BackendAPIService environment example

# DB mode: 'mongo' (requires Mongo variables) or 'memory' (no persistence)
DB_MODE=memory

# Mongo configuration (required if DB_MODE=mongo)
# MONGODB_URI=mongodb+srv://user:pass@cluster.example.mongodb.net/?retryWrites=true&w=majority
# MONGODB_DB_NAME=network_devices
# MONGODB_TLS=true

# Server bind
SERVER_HOST=0.0.0.0
SERVER_PORT=3001
