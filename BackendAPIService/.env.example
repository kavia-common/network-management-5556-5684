# BackendAPIService environment variables

# Preferred single-URI configuration
MONGODB_URI=mongodb://localhost:27017/network_devices

# Optional overrides
MONGODB_DB_NAME=network_devices
MONGODB_COLLECTION=device
# MONGODB_TLS=false
# MONGODB_CONNECT_TIMEOUT_MS=5000

# Optional: Fallback parts (used only if MONGODB_URI is not set)
# MONGODB_HOST=localhost
# MONGODB_PORT=27017
# MONGODB_USERNAME=
# MONGODB_PASSWORD=
# MONGODB_OPTIONS=
