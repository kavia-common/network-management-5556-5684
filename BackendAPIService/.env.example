# Preferred single-URI configuration (DB name can be part of URI or via MONGODB_DB_NAME)
# Defaults if not set:
#   MONGODB_URI -> mongodb://localhost:27017/network
#   MONGODB_DB_NAME -> network
#   MONGODB_COLLECTION -> device

MONGODB_URI=mongodb://localhost:27017/network
MONGODB_DB_NAME=network
MONGODB_COLLECTION=device
MONGODB_TLS=false
MONGODB_CONNECT_TIMEOUT_MS=5000

# Or construct from parts (used only if MONGODB_URI is not provided)
# MONGODB_HOST=localhost
# MONGODB_PORT=27017
# MONGODB_USERNAME=
# MONGODB_PASSWORD=
# MONGODB_OPTIONS=
