Bandit reports are generated by running:
    bandit -r . -f json -o reports/bandit_report.json
    bandit -r . -f txt -o reports/bandit_report.txt

Notes:
- High severity issues currently shown in reports originate from third-party dependencies inside the virtual environment (venv). Application code has no High severity findings as of this revision.
- We mitigated several Low severity findings by replacing asserts and try/except/pass blocks.
