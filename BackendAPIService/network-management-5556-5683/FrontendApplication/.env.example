# FrontendApplication environment variables example
# Copy to .env and set according to your backend deployment.
# IMPORTANT: Changes to REACT_APP_* variables require a rebuild of the React app.

# Base URL for the Backend API (no trailing slash)
# Example (local): http://localhost:3001
REACT_APP_API_BASE_URL=
